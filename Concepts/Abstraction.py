'''
Abstraction:Define the essential behavior while leaving the implementation details to subclasses
Python provides the abc module (Abstract Base Classes) to support abstraction.
from abc import ABC, abstractmethod
ABC → Used to create abstract classe: is a class that cannot be instantiated and is intended to be inherited
@abstractmethod → Used to create abstract method: is a method without an implementation


'''
